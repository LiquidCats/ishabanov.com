export enum ToolType {
    database = 'database',
    deploy = 'deploy',
    framework = 'framework',
    language = 'language',
    queue = 'queue',
    tool = 'tool',
}

export enum ToolLevel {
    junior,
    middle,
    senior,
    expert,
}
