version: "3.8"

services:
  app:
    image: r.liquid-cats.com:5000/onepager/app:latest
    deploy:
      mode: replicated
      replicas: 1
    ports:
      - "8000:80"

  cron:
    image: r.liquid-cats.com:5000/onepager/cron:latest
    deploy:
      mode: replicated
      replicas: 1

  redis:
    image: redis:latest
    deploy:
      mode: replicated
      replicas: 1
      resources:
        limits:
          memory: 128M
